<!--
show the picture of the game
show the description of the game
show the year of the game
show the owner of the game
show the reviews of the game
create a new review in the show of reviews (content/rating)
boardgame show edit button
boardgame show delete button
show a map where the game is located
 -->
<div class="container">
  <div class="card">
    <div class="card">
      <%= cl_image_tag @boardgame.photo, height: 300, width: 400, crop: :fill, alt: @boardgame.name %>
    </div>
    <div class="card">
      <ul>
        <li><%= @boardgame.name %></li>
        <li><%= @boardgame.description %></li>
        <li><%= @boardgame.year %></li>
      </ul>
    </div>
    <div class="card">
      <%#= render 'availability_toggle' %>
      <p>toggle should go here</p>
    </div>
  </div>

  <div class="card">
    <% @reviews = Review.last(5).reverse %>
    <% @reviews.each do | review| %>
      <div class="notification">
          <img src='https://kitt.lewagon.com/placeholder/users/arthur-littm' class="avatar-large"/>
          <%#= cl_image_tag @user.photo, alt: @user.last_name, @user.first_name %>
            <div class="notification-content">
              <% review.rating.times do |s| %>
              <i class="far fa-star"></i>
              <% end %>
              <p>"<%= review.content %>"</p>
            </div>
          <div class="notification-actions">
            <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
            <%= link_to 'Destroy', boardgame_path,
              data: { confirm: 'Are you sure?' }, method: :delete %>
        </div>
      </div>
    <% end %>

    <div class="notification">
      <img src='https://kitt.lewagon.com/placeholder/users/arthur-littm' class="avatar-large"/>
      <%#= cl_image_tag @user.photo, alt: @user.last_name, @user.first_name %>
      <div class="notification-content">
        <p><%#= render 'review_form' %></p>
      </div>

      <div class="notification-actions">
        <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
        <a href="#">Delete <i class="far fa-trash-alt"></i></a>
      </div>
    </div>
  </div>
</div>




