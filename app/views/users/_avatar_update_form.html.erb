<%= simple_form_for(@user) do |f| %>
  <%= f.input_field :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit, :value => "Update your picture" , :class => "btn btn-light photo-submit"%>
<% end %>
